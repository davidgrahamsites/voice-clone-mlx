"""macOS .app packaging for the two launchable apps.

Import `app_bundle` directly; this package re-exports nothing so adding a
packaging target never changes what importing the package pulls in.
"""

